import 'package:flutter/material.dart';
import 'package:live/screens/list_screen.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(      
      body: ListScreen(),
    );
  }
}